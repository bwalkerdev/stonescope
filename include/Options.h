#ifndef OPTIONS_H
#define OPTIONS_H
enum class PortNumber { One, Two, Three, Four, Five, Six };
enum class FrequencyType { GHz, MHz, KHz, Hz };

enum class ParameterType { S, Y, Z, H, G };

enum class ParameterFormat { DB, MA, RI };

enum class TouchstoneErrors { FileOpen };
#endif // !OPTIONS_H
